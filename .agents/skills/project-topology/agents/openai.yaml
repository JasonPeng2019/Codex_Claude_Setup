interface:
  display_name: "Project Topology"
  short_description: "Create detailed plans without process bloat."
  default_prompt: "Use $project-topology to create the stable PLAN.md plus STEP-* package with repository-grounded technical detail. Validate required behavior rather than administrative perfection, preserve valid progress, add process only for concrete needs, and run the ROOT-adjudicated Bullshit Checker loop until no valid overcomplexity findings remain."
policy:
  allow_implicit_invocation: false
