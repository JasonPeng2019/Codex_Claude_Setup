interface:
  display_name: "Project Topology"
  short_description: "Route work and compile modular execution plans."
  default_prompt: "Use $project-topology to audit project truth, choose the least complex justified execution level, and, when formal Level 4 is selected, emit and validate the exact modular plan/workflow package with isolated global rules, independent STEP files composed from authoritative M01-M10 modules, a separate role-agent mapping, runtime IDs only for multi-agent runtime instances, complete ROOT-authored direct-worker contracts, open review findings inside concrete assigned boundaries, unbounded agent sessions, and no IDs or hashes for ordinary content."
policy:
  allow_implicit_invocation: false
