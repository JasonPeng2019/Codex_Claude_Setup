<!-- Drafting aid: fill inside the existing verification/block/card; reference one owner.
     In formal Level 4, map these prompts to existing fields, preserving exact schemas.
     Read references/matrix-execution.md for the governing contract. -->

Matrix / owning step or gate:
Required claims and concrete assertions (reference accepted scope):
Runner entrypoint / input snapshot / control readiness evidence or prerequisite:
Decision owner / delegated lane authority if permitted / parent join and shared budget:
Concurrency limit and basis / shared-resource constraints:

| Coordinate or equivalent family | Command / oracle | Prerequisite IDs and required output/state | Isolated resources / cleanup owner | Success and incompatible terminal predicates / detection source | Expected seconds / finite maximum / cleanup allowance / estimate basis |
| --- | --- | --- | --- | --- | --- |

Expand families to stable coordinate IDs for result collection. Families may share
one row only when their graph, resources, terminal handling and budgets are equivalent.
Declare independent roots explicitly and preserve each coordinate's own result.

Scheduling: ready graph nodes run concurrently within resource/capacity constraints;
name any serialized edge and its cause. State detection latency and immediate exit route.

Collection: ordinary failures do not stop independent coordinates. Name the complete
result location, blocked/unrun accounting, exceptional stop scope and cleanup deadline.
Hold the snapshot stable; begin repairs only after feasible collection finishes.

Repair: group all findings by evidenced root cause; one owner and repair assignment per
cause group. Record uncertain diagnoses and protect overlapping write boundaries.

Rerun: changed-input mapping, invalidated coordinates and dependents, outstanding
blocked/unrun requirements, and preserved compatible PASS evidence.

Wall clock: expected total range and maximum, graph critical path, setup/cleanup,
concurrency/contention assumptions, estimate basis, overrun/exhaustion route and actual
elapsed collection. Keep test deadlines separate from agent-session progress monitoring.

Review: existing independent reviewer checks coverage and rejects unnecessary serial
execution, terminal-state waits and per-test repair loops; record writer dispositions
and ROOT acceptance in the existing scope-audit record.
